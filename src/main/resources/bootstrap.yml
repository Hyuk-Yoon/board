logging:
  config: classpath:logback-${spring.profiles.active}.xml

spring:
  config:
    activate:
      on-profile: production
  cloud:
    config:
      uri: http://blogo.servegame.com:8888
      name: cloud-config
